{
  "name": "dealflow360",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "npm --prefix Frontend run build",
    "dev": "npm --prefix Frontend run dev"
  },
  "dependencies": {
    "@neon/config": "^1.5.0",
    "@neon/env": "^1.3.4",
    "pdf-parse": "^2.4.5"
  }
}
